import AutoMobile from './OOPMobiles.js';
export default class Full extends AutoMobile{
  constructor (model, color, doors, engineCyl, seating, sunRoof, transmission, gas) {
    super(model, color, 'big', doors, 'big', engineCyl, seating, sunRoof, 'big', transmission, gas); {

    }
  }
}
