import AutoMobile from './OOPMobiles.js';
export default class Full extends AutoMobile{
  constructor (model, color, engineCyl, gas) {
    super(model, color, 'big', 2, 'big', engineCyl, 'maximum', false, 'big', 'manual', gas); {

    }
  }
}